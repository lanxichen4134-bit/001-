# 澜心内容 Brain 仓库接手说明

## 1. 仓库目标与边界

本仓库是澜心内容 Brain V0.1 的长期执行层：承接灵感、记录已确认的长期内容事实、支持内容共创与复盘，并提供可上传到 ChatGPT Project 的同步快照。

- 远端唯一目标仓库：001-，当前分支为 main。
- 旧 Obsidian Vault 是既有资产的只读来源，不得因本项目自动写入、提交或推送到其旧远端。
- 本仓库不是旧 Obsidian Vault 的完整复制品。研究正文、原始语音、完整聊天、学员资料、个人隐私和大文件留在其受控原位置；这里只保留脱敏的索引、结论和可公开使用的结构化信息。
- 远端目前为公开仓库。任何准备提交的内容都必须按公开可见标准审查。

## 2. 每次任务开始前必须检查

1. 本文件。
2. 澜心内容Brain/Docs/PROJECT_STATE.md。
3. 本次任务相关的 Brain 文件、原始资料和已有内容卡。
4. git rev-parse --show-toplevel、git branch --show-current、git remote -v、git status --short。

若远端不是本项目指定仓库、工作区存在无关未提交变更，或任务需要覆盖原始资料，停止写入并说明原因。

## 3. 事实优先级

1. 澜心本次最新、明确的输入。
2. 已明确确认的 Brain 条目。
3. 本仓库正式已提交文件与 Docs/DECISIONS.md。
4. 保留原貌的原始灵感与原始资料。
5. 历史摘要。
6. AI 推测。

发生冲突时，不要悄悄融合；保留来源、记录变化，并让最新明确输入优先。

## 4. 原话与 Brain 保护

- Inbox 中的原话不得覆盖、润色后替换、删除或改写后冒充原话。
- AI 理解、标题、标签和候选观点必须单独保存。
- 核心身份、价值观、经典观点、禁用表达只能在有澜心明确确认依据时升级为长期事实；否则记录为候选或研究中。
- 不得把 AI 生成句、他人发言或未经确认的转写混入澜心原话库。

## 5. 内容协作与专业边界

默认路径：灵感 → 讨论 → 观点 → 选题 → 三个开头 → 结构 → 澜心确认 → 成稿 → 发布 → 复盘。仅在澜心明确要求“直接写”时跳过讨论。

禁止虚构经历、案例、用户反馈、数字、平台成绩、医学研究、机构关系、头衔、来源或 GitHub 完成状态。科学事实、文化解释、个人体验和待验证推断必须分层。不得制造身体羞耻、焦虑营销、低俗擦边、医疗诊断/疗效承诺或过度营销。

## 6. 文件执行原则

- 最小修改，优先引用既有资产；不为整洁而大规模重命名、搬迁、删除或复制正文。
- 一份正文只保留一个主位置；跨用途使用链接或索引。
- 涉及学员、客户、家庭、健康、语音、影像、聊天记录或未公开经营数据时，默认去标识化；原始材料不进入本仓库。
- 提交前检查 .gitignore 和 staged diff，确保不存在 Token、密码、Cookie、私钥、访问链接或本机敏感路径。

## 7. GitHub 最小闭环

每次有文件执行时，完成下列闭环：

1. 最小更新 PROJECT_STATE.md，真实记录本次动作、完成项与待办。
2. 只用 git add -- <本次实际修改路径> 暂存；严禁 git add . 或 git add -A。
3. 使用清晰的 content-brain: 前缀提交信息。
4. 执行普通 git push origin <当前分支>；不得 force push。
5. 比较 git rev-parse HEAD 与 git ls-remote origin refs/heads/<当前分支>，确认 Local HEAD = Remote HEAD。

禁止 git reset --hard、git clean -fd、git checkout -- .、force push，除非澜心明确逐项授权。

## 8. GitHub 与多电脑协作原则

- GitHub `origin/main` 是本项目唯一正式事实源；不以任何一台电脑的本地副本作为项目主版本。
- 家里电脑与当前电脑不手工复制正式项目文件，统一通过 GitHub 完成版本同步；`AGENTS.md` 本身也以 GitHub `main` 的正式版本为准。
- 每次开始正式工作前，先 `git fetch origin`，并检查本地与 `origin/main` 的分支、工作区和提交差异。
- 仅在工作区干净且不存在本地未提交修改、本地独有 commit 或分叉时，才同步最新 `main`；发现任一情况时，禁止直接覆盖，先保护并报告。
- 正式规则、Brain、Content、Docs、Inbox、Reviews、Prompts 与 Project资料同步包不得长期只存在某一台电脑。
- 正式修改完成后，必须 commit、`git push origin main`，并比较 Local HEAD 与 `git ls-remote origin refs/heads/main`，确认两者相等。

## 9. 视频内容档案与数据记录

- 视频内容任务开始前，必须读取 `澜心内容Brain/Docs/VIDEO_CONTENT_SYSTEM.md` 及对应模板。
- 一条内容只能有一个正式 Content ID；同一内容多平台发布不得重复建立 Content，而应建立不同 Publish ID。
- Content status 继续使用现有唯一状态链；`media_stage` 只记录媒体制作进度，不得另建视频状态系统。
- 发布数据采用追加式 Data Snapshot，绝不覆盖历史值；平台原始数据与 AI 计算指标必须分开。
- 逐字稿必须区分原始机器转写、AI 核对版、AI 整理版；AI 改写不得冒充澜心原话。
- 候选长期洞见、候选原话和 AI 建议不得自动升级到 Brain；必须等待澜心明确确认。
- 原始视频、后台截图、本机绝对路径及未脱敏的反馈不进入公开仓库。本地媒体缺失或不可读取时，不得假装已核对或已转写视频。
- GitHub `origin/main` 是视频文本档案和数据记录的唯一正式事实源；媒体资产的跨电脑访问使用受控媒体存储。
